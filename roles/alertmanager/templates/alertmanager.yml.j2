global:
 resolve_timeout: 1m
 smtp_require_tls: False

route:
  group_by:
  - '...'
  receiver: 'email'
  routes:
  - match:
      severity: email
    receiver: email

receivers:
- name: 'email'
  email_configs:
    - to: '{{ email }}'
      from: '{{ email }}'
      smarthost: smtp.gmail.com:587
      auth_username: '{{ email }}'
      auth_identity: '{{ email }}'
      auth_password: '{{ email_pass }}'
      send_resolved: true
